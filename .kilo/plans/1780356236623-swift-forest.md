# Global Plan: TODO-08 — Contact Section + Footer

**Source**: `.agent/todos/20260529/20260529-todo-3.md`
**Date**: 2026-06-01

---

## Task Breakdown (Pattern C: 5 tasks from `## Tasks > ###` headings)

| # | Task | Scope |
|---|------|-------|
| T1 | Create Contact Section Component | Full ContactSection.vue implementation |
| T2 | Create Footer Component | Full Footer.vue implementation |
| T3 | Design & UX | Verify visual consistency, responsive form, CTA impact |
| T4 | Integrate into Main App | Already done — verify no regressions |
| T5 | Styling & Polish | Final spacing, typography, color consistency pass |

---

## Execution Plan

### Step 2: Git Feature Branch Setup → `subagent_type: implementer`
- Commit any unstaged changes, checkout `main`, create `feat/contact-footer-section`

### Step 3: Version Update → `subagent_type: implementer`
- Bump patch version in `package.json`

---

### T1: Create Contact Section Component

#### T1 — 4.1 Analysis & Planning → `subagent_type: architect`
Analyze and generate detailed implementation plan covering:
- Component structure, content (Spanish from `landing-content.es.md` §10 — heading "¿Listo para probar Cobranza App?"), email `cobranza360pro@gmail.com`
- Google Form `<iframe>` embed with responsive wrapper
- CTA button, scroll-reveal animations, dark-first styling per `style-guide.md` §3.1 (background: `--color-bg-dark`)
- Mention that forms save to Google Sheets
- Save to `.kilo/plans/20260601-contact-section.md`

#### T1 — 4.2 Implementation → `subagent_type: implementer`
Implement `src/components/landing/ContactSection.vue` per plan.

#### T1 — 4.3 Code Review → `subagent_type: code-reviewer`
Review for errors/deviations from plan. Max 3 fix cycles.

#### T1 — 4.4 Documentation → `subagent_type: docs-specialist`
Add code comments, update project context.

#### T1 — 4.5 Verification → `subagent_type: implementer`
Verify plan adherence, commit.

#### T1 — 4.6 Task Completion → `subagent_type: implementer`
Mark T1 as `[DONE]` in TODO file.

---

### T2: Create Footer Component

#### T2 — 4.1 Analysis & Planning → `subagent_type: architect`
Analyze and generate plan covering:
- Copyright, accompanying phrase ("Un sistema en constante evolución…"), contact email repeated
- Optional: links to main sections, legal disclaimer
- Clean professional footer design, `--color-bg-deepest` background
- Save to `.kilo/plans/20260601-footer.md`

#### T2 — 4.2 Implementation → `subagent_type: implementer`
Implement `src/components/landing/Footer.vue` per plan.

#### T2 — 4.3 Code Review → `subagent_type: code-reviewer`
Review for errors/deviations. Max 3 fix cycles.

#### T2 — 4.4 Documentation → `subagent_type: docs-specialist`
Add code comments, update project context.

#### T2 — 4.5 Verification → `subagent_type: implementer`
Verify plan adherence, commit.

#### T2 — 4.6 Task Completion → `subagent_type: implementer`
Mark T2 as `[DONE]` in TODO file.

---

### T3: Design & UX

#### T3 — 4.1 Analysis & Planning → `subagent_type: architect`
Analyze both components against style guide requirements:
- Contact section visually strong as final CTA
- Clean professional footer
- Embedded form responsive on mobile
- Good contrast and readability
- Save to `.kilo/plans/20260601-design-ux-review.md`

#### T3 — 4.2 Implementation → `subagent_type: implementer`
Apply design tweaks per plan (CSS/media query adjustments).

#### T3 — 4.3 Code Review → `subagent_type: code-reviewer`
Review design adjustments.

#### T3 — 4.4 Documentation → `subagent_type: docs-specialist`
Update style guide if new patterns emerged.

#### T3 — 4.5 Verification → `subagent_type: implementer`
Visual verification pass, commit.

#### T3 — 4.6 Task Completion → `subagent_type: implementer`
Mark T3 as `[DONE]` in TODO file.

---

### T4: Integrate into Main App

#### T4 — 4.1 Analysis & Planning → `subagent_type: architect`
Verify App.vue already has both components. Plan: likely no code changes needed, just confirmation.

#### T4 — 4.2 Implementation → `subagent_type: implementer`
If needed, adjust App.vue. Otherwise confirm and skip.

#### T4 — 4.3 through 4.5 → skipped if no code changes.

#### T4 — 4.6 Task Completion → `subagent_type: implementer`
Mark T4 as `[DONE]` in TODO file.

---

### T5: Styling & Polish

#### T5 — 4.1 Analysis & Planning → `subagent_type: architect`
Final review pass: consistent spacing, colors, typography, page flow, mobile responsiveness.

#### T5 — 4.2 Implementation → `subagent_type: implementer`
Apply final polish tweaks.

#### T5 — 4.3 Code Review → `subagent_type: code-reviewer`
Review polish changes.

#### T5 — 4.4 Documentation → `subagent_type: docs-specialist`
Final context update.

#### T5 — 4.5 Verification → `subagent_type: implementer`
Build check, TypeScript check, commit.

#### T5 — 4.6 Task Completion → `subagent_type: implementer`
Mark T5 as `[DONE]` in TODO file.

---

### Step 5: TODO File Completion → `subagent_type: implementer`
- Rename TODO file with `-DONE` suffix
- Merge feature branch to `main`, push to `origin`

---

## Key Constraints (from project info)

- **Language**: Code/comments in English, user-visible text in neutral Spanish
- **Content**: Spanish texts from `landing-content.es.md` §9 and §10
- **Styling**: Use CSS variables only, follow dark-first pattern from `style-guide.md`
- **Contact Section BG**: `--color-bg-dark` (row 9 in alternation table)
- **Footer BG**: `--color-bg-deepest` (row 10)
- **Scroll Reveal**: Import `useScrollReveal()` composable, add `data-reveal` attributes
- **Embeds**: Google Form `<iframe>` — make responsive
- **File limits**: <200 lines per source file, <50 lines per method, max depth 2
