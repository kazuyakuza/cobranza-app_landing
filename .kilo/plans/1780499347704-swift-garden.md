# Plan: 20260603 — Landing Page Quick Fixes (3 Tasks)

**TODO File:** `.agent/todos/20260603/20260603-todo-0.md`
**Date:** 2026-06-03
**Status:** Pending Approval

---

## Pre-Analysis

Three small, scoped changes to existing landing page components. All are UI/text edits in Vue single-file components. No new dependencies, no structural changes.

- **Task 1 (Navbar mobile reorder):** Swap hamburger and CTA button order in the mobile flex container.
- **Task 2 (ProblemSection text):** Single string replacement in `painPoints` array.
- **Task 3 (ContactSection):** Change CTA button label + add pre-form text line.

---

## Global Plan (Critical Workflow Steps)

| Step | Agent | Description |
|------|-------|-------------|
| 2 | implementer | Git Feature Branch Setup |
| 3 | implementer | Version Update (patch bump) |
| 4.1 | architect | Task 1: Analysis & Planning (Navbar mobile reorder) |
| 4.2 | implementer | Task 1: Implementation |
| 4.3 | code-reviewer | Task 1: Code Review |
| 4.4 | docs-specialist | Task 1: Documentation |
| 4.5 | implementer | Task 1: Verification |
| 4.6 | implementer | Task 1: Task Completion [DONE] |
| 4.1 | architect | Task 2: Analysis & Planning (ProblemSection text) |
| 4.2 | implementer | Task 2: Implementation |
| 4.3 | code-reviewer | Task 2: Code Review |
| 4.4 | docs-specialist | Task 2: Documentation |
| 4.5 | implementer | Task 2: Verification |
| 4.6 | implementer | Task 2: Task Completion [DONE] |
| 4.1 | architect | Task 3: Analysis & Planning (ContactSection changes) |
| 4.2 | implementer | Task 3: Implementation |
| 4.3 | code-reviewer | Task 3: Code Review |
| 4.4 | docs-specialist | Task 3: Documentation |
| 4.5 | implementer | Task 3: Verification |
| 4.6 | implementer | Task 3: Task Completion [DONE] |
| 5 | implementer | TODO File Completion (rename with -DONE, merge to main, push origin) |

---

## Task 1: Reorder Navbar in Mobile Version

### Context
File: `src/components/landing/Navbar.vue`
The mobile layout currently renders: **brand → hamburger → CTA button**.
User wants: **brand → CTA button → hamburger**.

### Target State
The `d-flex d-lg-none` container (lines 74-93) reorders its children so the `navbar-toggler` (hamburger) appears **after** the `mobile-cta` button.

### Implementation
Swap the order of the two elements inside `<div class="d-flex align-items-center gap-2 d-lg-none">`:
1. Move `<a class="btn btn-success ... mobile-cta">` **before** `<button class="navbar-toggler">`
2. No logic, style, or script changes needed. Pure template reorder.

---

## Task 2: Change ProblemSection Text

### Context
File: `src/components/landing/ProblemSection.vue`, line 29.

### Change
```
- text: 'Conciliar manualmente con los extractos bancarios'
+ text: 'Conciliar manualmente con los movimientos bancarios'
```
Single string literal replacement. No structural changes.

---

## Task 3: ContactSection Changes

### Context
File: `src/components/landing/ContactSection.vue`

### Change A — CTA Button Label (line 12)
```
- const ctaText = 'Quiero probar la Beta gratis'
+ const ctaText = 'Enviar Correo'
```
Keep the existing `ctaHref` with its `mailto:` subject intact.

### Change B — Pre-form Text (before Google Form iframe)
Insert a new `<p>` element between the CTA button and the form wrapper:

```html
<p data-reveal class="contact-pre-form-text">
  O bien deja tus datos y te contactamos:
</p>
```

Add minimal CSS:
```css
.contact-pre-form-text {
  font-size: 1rem;
  color: var(--color-text-on-dark-muted);
  margin-bottom: 1rem;
}
```

Position in template: after the CTA `<a>` link (line 45) and before the `<div class="contact-form-wrapper">` (line 47).
