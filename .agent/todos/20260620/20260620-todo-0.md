- in the "Cómo funciona Cobranza App" section, step 5, modify the graph so: instead of "Cliente -> Deuda", "Cliente -> Comprobante"; and improve the visual aspect of the graph, specially the arrow from "Extracto Bancario" to "Transferencia"
- in "Cómo te ayuda Cobranza App" section, replace "Un portal web simple donde el cliente puede:" to "Un portal web sencillo de usar, donde:".
- in FAQ section, the styles of the open accordion group is not visible. When open, the text color switches from white to dark blue, but bkg is blue.
- for the mobile version, the nav-bar width don't sets to the screen width, and that broken the web view. Or maybe it is just that the graph in the "Cómo funciona Cobranza App" section, step 5, overflows and broken the page. Maybe the changes of the fist task fix this, however the graph container should not broken the page. We can set to scroll inside the graph container to see it; or another better option is to reduce the graph size, and when the user touches the graph, a popup shows it more big, maybe?.
- for the mobile version, the "Solicitar acceso anticipado gratuito" btn floating no btn must be visible in all sections, except in the fist (top) section (ie. "Inicio" section) and last (bottom) section (ie. "Contacto" section).
- for the mobile version, in the table under "¿Por qué no alcanza con Excel o WhatsApp?", reduce the text size, just a little, so I don't need to horizontally scroll to completely view the table.
- for the mobile version, the accordion groups under "Seleccioná tu caso" style is wrong. It is the same error described for the FAQ section, but this is only for the mobile version which uses accordion). Also, there is another style error: the content inside of each accordion has not top padding.
