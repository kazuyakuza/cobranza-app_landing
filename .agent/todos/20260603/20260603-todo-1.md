- Remove mobile CTA from navbar + add menu auto-close on outside click and scroll
- Create FloatingCta.vue component — fixed bottom-right, hidden on hero/contact sections
- Increase HeroSection mobile top padding + Footer mobile bottom padding
- App.vue — import and render FloatingCta component