# TODO-01: Project Initial Setup [DONE]

## Objective

Perform the initial setup of the **Cobranza App Landing Page** project. This is the foundational step before building any UI components.

**Important Notes:**

- All code, comments, and file names must be in **English**.
- All user-visible text must come from the Spanish content file.
- Keep the project clean, well-organized and maintainable.

## Tasks

### 1. Update Project Documentation

- [x] Update `README.md` with proper project information:
  - Project title and description
  - Technology stack (Vue 3 + TypeScript + Bootstrap 5)
  - Language rules (Spanish for UI, English for code)
  - How to run the project (development & build commands)
  - Project structure overview
  - Link to the Project Brief

### 2. Configure package.json

- [x] Update `package.json` with:
  - Proper project name: `cobranza-app-landing`
  - Version: `0.1.0`
  - Description
  - Author
  - License
  - Correct scripts (`dev`, `build`, `preview`, `lint`, etc.)
  - Dependencies and devDependencies section ready

### 3. Define Project Structure

Create the following folder structure (if not already present):

```text
src/
├── assets/
│   ├── images/
│   └── icons/
├── components/
│   └── landing/          # All landing page sections
│       ├── HeroSection.vue
│       ├── ProblemSection.vue
│       ├── SolutionSection.vue
│       ├── UseCasesSection.vue
│       ├── FeaturesSection.vue
│       ├── BetaSection.vue
│       ├── PricingSection.vue
│       ├── FaqSection.vue
│       ├── ContactSection.vue
│       └── Footer.vue
├── composables/
├── types/
├── utils/
├── App.vue
└── main.ts
public/
├── favicon.ico
└── ...
```

### 4. Initialize Vue 3 + TypeScript Project

- [x] Ensure the project is properly set up with Vite + Vue 3 + TypeScript
- [x] Install last version of core dependencies:
  - vue and dependencies
  - TypeScript
  - Vite
  - Bootstrap 5 + Bootstrap Icons

### 5. Add and Configure Bootstrap 5

- [x] Install Bootstrap 5 and Bootstrap Icons
- [x] Import Bootstrap CSS and JS properly in `main.ts`
- [x] Configure Bootstrap to work with Vue

### 6. Project Configuration

- [x] Configure `tsconfig.json` (strict mode recommended)
- [x] Configure `vite.config.ts` with proper aliases and settings
- [x] Set up ESLint + Prettier (if not already configured)
- [x] Add basic environment variables structure (`.env.example`)

### 7. Initial Code Cleanup

- [x] Remove any unnecessary template files or components
- [x] Update `App.vue` to a clean starting point (basic layout with navbar placeholder)
- [x] Ensure the project builds without errors